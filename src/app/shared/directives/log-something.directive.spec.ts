import { LogSomethingDirective } from './log-something.directive';

describe('LogSomethingDirective', () => {
  it('should create an instance', () => {
    const directive = new LogSomethingDirective();
    expect(directive).toBeTruthy();
  });
});
